# Windows 10 部署说明

## 1. 准备服务器电脑

选择一台白天持续开机、接入公司局域网的 Windows 10 电脑。

- 建议内存 4 GB 以上。
- 关闭工作时间自动睡眠。
- 最好在路由器中为这台电脑保留固定内网 IP。

## 2. 安装 Python

安装 64 位 Python 3.12 或 3.13。安装界面务必勾选 `Add Python to PATH`。

安装完成后打开命令提示符，执行：

```bat
py --version
```

能显示版本号即可。

## 3. 复制系统

将整个项目文件夹复制到固定位置，例如：

```text
D:\发货清单
```

不要只复制 `app.py`，模板、静态文件和启动脚本都需要保留。

## 4. 首次启动

双击：

```text
start-windows.bat
```

第一次会自动建立 `.venv` 并下载运行依赖，时间可能稍长。完成后浏览器打开：

```text
http://127.0.0.1:8765
```

Windows 防火墙第一次询问时，只勾选“专用网络”并允许访问，不要开放到公用网络。

## 5. 让其他内网电脑访问

在服务器电脑的命令提示符执行：

```bat
ipconfig
```

找到正在使用的网卡的 `IPv4 地址`，例如 `192.168.1.50`。

其他内网电脑在浏览器打开：

```text
http://192.168.1.50:8765
```

所有设备必须处在能互相访问的同一公司局域网中。

## 6. 数据和备份

业务数据保存在：

```text
data\packing.db
```

每天的最新备份保存在：

```text
data\backups
```

原始上传文件保存在：

```text
data\uploads
```

建议每天将整个 `data` 文件夹再复制到另一块磁盘或公司共享盘。

## 7. 日常启动和关闭

- 启动：双击 `start-windows.bat`。
- 关闭：关闭标题为“发货清单”的服务窗口。
- 服务器电脑关闭或睡眠后，其他电脑无法访问。

## 8. 常见问题

### 本机能打开，其他电脑打不开

依次检查：

1. 两台电脑是否在同一内网。
2. 使用的是否是服务器电脑 IPv4 地址，而不是 `127.0.0.1`。
3. Windows 网络类型是否为“专用网络”。
4. 防火墙是否允许 Python 或 8765 端口的专用网络访问。
5. 服务器电脑是否进入睡眠。

### 双击启动文件立即关闭

打开命令提示符，进入项目目录后执行：

```bat
start-windows.bat
```

保留窗口中的错误文字用于排查。
