# 发货清单技术方案

## 1. 当前版本

- 试运行分支：`release/ip-whitelist-pilot`
- 使用方式：无账号登录，仅允许配置的公网 IP 访问
- 本地地址：`http://127.0.0.1:8080`
- 服务器：Docker 部署，PostgreSQL 持久化，Caddy 负责 HTTPS 与 IP 白名单
- 当前状态：本地与服务器代码一致；服务器公网入口需在阿里云安全组开放 80、443 后才能访问

本分支用于业务人员验收。确认无误后合并到 `main`，再作为正式版本运行。

## 2. 业务操作流程

1. 操作员点击“导入新批次”，选择聚水潭拣货单，并填写一次内部单号。
2. 系统生成日期流水批次号，解析本批次款色尺码与清单数量。
3. 操作员输入大包号。只需输入自然数，系统自动显示为 `1#`、`2#`。
4. 点击款色尺码输入框即展开当前批次商品；可输入多个不连续关键词，例如 `001 xl`。
5. 选择商品后，数量旁显示当前大包录入过程中的参考剩余库存。
6. 输入数量并添加。添加完成后，下拉框自动收起，但焦点仍停在款色尺码，方便继续输入下一款。
7. 一个大包可录入多个款色尺码。删除或修改已添加商品时，参考库存立即返还或重新计算。
8. 长、宽、高、重量允许留空；已保存大包明确显示“长/宽/高未填”和“体重未填”。
9. 同款大包数大于 1 时，系统复制商品、数量、长宽高重，并连续生成大包号。
10. 超过本批次清单数量时只提醒，经二次确认后仍可强制保存。
11. 可按批次号点击“查询”加载历史批次；未输入搜索条件时只显示最近 3 个批次。
12. 点击“导出 Excel”下载当前批次，页面显示下载文件名与状态。

## 3. 系统架构

```text
操作员浏览器
    │ HTTPS 80/443
    ▼
Caddy
    ├─ 自动 HTTPS
    ├─ 来源 IP 白名单
    └─ 安全响应头
    │ Docker 内部网络
    ▼
Flask + Waitress
    ├─ Excel 导入/导出
    ├─ 批次与大包业务规则
    ├─ CSRF 校验
    └─ 参数化 SQL
    │ Docker 内部网络
    ▼
PostgreSQL
```

Docker 服务：

- `caddy`：唯一公网入口，开放 80、443。
- `app`：应用端口仅绑定服务器 `127.0.0.1:8080`。
- `db`：PostgreSQL 不映射宿主机端口，只允许应用容器访问。

## 4. 技术选型

| 层级 | 方案 | 原因 |
| --- | --- | --- |
| 页面 | 原生 HTML/CSS/JavaScript | 依赖少，内网页面维护简单 |
| 后端 | Python Flask + Waitress | 代码量小，适合 Excel 与轻量业务接口 |
| 数据库 | PostgreSQL | 支持服务器长期运行、事务与并发访问 |
| Excel | openpyxl | 解析聚水潭 `.xlsx` 并生成发货清单 |
| 部署 | Docker Compose | Mac、服务器运行方式一致，便于重启和更新 |
| 公网入口 | Caddy | 自动 HTTPS、反向代理和 IP 白名单 |
| 版本管理 | Git | 试运行分支验收后再合并正式版本 |

## 5. 数据设计

- `batches`：批次号、内部单号、来源文件、创建与更新时间。
- `skus`：批次内商品编码、商品名、款式、颜色尺码、仓位、清单数量、搜索聚合字段。
- `packages`：大包号、可选长宽高重、所属批次。
- `package_items`：大包与 SKU 的数量明细。

关键约束：

- 批次号全局唯一。
- 同一批次的大包号唯一。
- 同一批次的商品编码唯一。
- 同一大包内同一 SKU 只保存一条，重复添加时合并数量。
- 批量生成大包使用数据库事务；任一大包号冲突时整次操作不落库。

## 6. 库存口径

系统只校验本次导入批次，不连接或校验聚水潭总库存。

```text
库存 = 本批次清单数量 - 本批次已保存数量
录入参考库存 = 库存 - 当前编辑区尚未保存的数量
```

库存可以为负数，代表本批次已超装。超装是软提醒，不是强制拦截。

## 7. Excel 导出

导出文件名：`发货清单_批次号.xlsx`，服务器响应中附带精确到分钟的实际生成文件名。

工作表包括：

1. `发货清单`：批次号、内部单号、大包总数、总件数、已填总重量、大包商品明细和长宽高重。
2. `SKU库存汇总`：商品字段、仓位、清单数量、已装数量、库存和状态。

导出接口返回标准 `.xlsx` 文件，并设置禁止缓存，避免下载到旧批次内容。

## 8. 安全方案

- 当前不设置登录账号，访问控制由 Caddy 的 `ALLOWED_CIDRS` 完成。
- 试运行仅配置当前操作电脑的公网 IP `/32`。
- 所有写操作校验 CSRF Token。
- SQL 用户值全部通过参数绑定传入，不直接拼接到 SQL。
- PostgreSQL 不开放公网端口。
- 应用端口只监听服务器回环地址。
- 启用 CSP、禁止页面被嵌入、禁止 MIME 猜测、关闭摄像头/麦克风/定位权限。
- `.env`、数据库、上传文件、备份、Excel 和本地运行环境不进入 Git。
- 上传文件限制为 `.xlsx`，请求体最大 20 MB。

注意：办公网络公网 IP 变化后，需要修改服务器 `.env` 中的 `ALLOWED_CIDRS` 并重启 Caddy。

## 9. 部署与数据持久化

服务器目录为 `/opt/shipping-list`，运行：

```bash
docker compose up -d --build
docker compose ps
```

数据保存在 Docker 命名卷：

- `postgres-data`：PostgreSQL 数据。
- `packing-uploads`：上传与应用数据目录。
- `caddy-data`、`caddy-config`：HTTPS 证书和 Caddy 状态。

`docker compose down` 不会删除数据；禁止执行 `docker compose down -v`。

备份脚本 `backup-postgres.sh` 使用 `pg_dump` 生成压缩文件，默认保留 14 天。恢复前必须停止业务写入并先备份当前数据库。

## 10. 测试与上线门槛

每次上线前必须通过：

```bash
.venv/bin/python -m unittest discover -s tests -v
npm run test:frontend
docker compose config --quiet
docker compose ps
```

当前自动测试覆盖：

- Excel 导入、内部单号、批次生成和历史查询。
- 多 SKU 大包、同款大包复制、冲突时事务回滚。
- 超装提醒、强制保存、修改、删除和库存返还。
- 长宽高重留空、CSRF 与安全响应头。
- Excel 两张工作表及表头数据。
- 款色尺码点击展开、多关键词搜索、添加后收起、库存扣减与返还。
- 导出地址和下载文件名。

试运行验收通过后：

1. 将 `release/ip-whitelist-pilot` 合并到 `main`。
2. 服务器切回 `main` 并重新构建应用容器。
3. 再次执行健康检查、真实导入、录入、导出冒烟测试。
4. IT 提供公司出口公网 IP 或网段后，更新 `ALLOWED_CIDRS`。

## 11. 当前限制

- 不含账号、角色和操作审计；安全边界依赖来源 IP。
- 不连接聚水潭 API，只处理人工导入的拣货单。
- 不校验企业总库存，只校验当前批次。
- 多人同时编辑同一大包时不做前端锁定，数据库唯一约束负责阻止重复大包号。
- 公网 HTTPS 首次启用前，阿里云安全组必须允许 80、443，以便证书验证和用户访问。
