# 发货清单技术方案

## 1. 当前版本

- 生产主线：`main`
- 稳定回滚标签：`v1.0.0`
- 使用方式：全站统一用户名和密码，可从任意公网 IP 访问
- 本地地址：`http://127.0.0.1:8080`
- 服务器：Docker 部署，PostgreSQL 持久化，Caddy 负责 HTTPS 与全站密码验证
- 当前状态：功能分支增加全站统一密码；部署时需要在阿里云安全组向公网开放 80、443

配比功能只在功能分支验收，不直接修改稳定分支。项目尚未正式上线；验收确认后才合并到 `main` 并发布 `v1.1.0`。

## 2. 业务操作流程

1. 操作员点击“导入新批次”，选择聚水潭拣货单，并填写一次内部单号。
2. 系统生成日期流水批次号，解析本批次款色尺码与清单数量。
3. 操作员输入大包号。只需输入自然数，系统自动显示为 `1#`、`2#`。
4. 点击款色尺码输入框即展开当前批次商品；可输入多个不连续关键词，例如 `001 xl`。
5. 选择商品后，数量旁显示当前大包录入过程中的参考剩余库存。
6. 如有固定装法，可先在当前批次的“配比管理”建立多个配比。每个配比由若干款色尺码及每中包件数组成，只在本批次可用。
7. 配比保存后与普通款色尺码显示在同一个下拉框；配比标明每中包总件数，悬停、聚焦或选中时可查看组成明细。
8. 选择普通商品时数量代表件数；选择配比时数量代表中包数量，系统自动换算实际总件数。
9. 输入数量并添加。一个大包可混装多个配比和普通 SKU；删除或修改条目时，参考库存立即返还或重新计算。
10. 长、宽、高、重量允许留空；已保存大包明确显示“长/宽/高未填”和“体重未填”。
11. 同款大包数大于 1 时，系统复制全部普通商品、配比及长宽高重，并连续生成大包号。
12. 超过本批次清单数量时只提醒，经二次确认后仍可强制保存。
13. 可按批次号点击“查询”加载历史批次；未输入搜索条件时只显示最近 3 个批次。
14. 点击“导出 Excel”下载当前批次，页面显示下载文件名与状态。

## 3. 系统架构

```text
操作员浏览器
    │ HTTPS 80/443
    ▼
Caddy
    ├─ 自动 HTTPS
    ├─ 全站统一密码验证
    └─ 安全响应头
    │ Docker 内部网络
    ▼
Flask + Waitress
    ├─ Excel 导入/导出
    ├─ 批次与大包业务规则
    ├─ CSRF 校验
    └─ 参数化 SQL
    │ Docker 内部网络
    ▼
PostgreSQL
```

Docker 服务：

- `caddy`：唯一公网入口，开放 80、443。
- `app`：应用端口仅绑定服务器 `127.0.0.1:8080`。
- `db`：PostgreSQL 不映射宿主机端口，只允许应用容器访问。

## 4. 技术选型

| 层级 | 方案 | 原因 |
| --- | --- | --- |
| 页面 | 原生 HTML/CSS/JavaScript | 依赖少，内网页面维护简单 |
| 后端 | Python Flask + Waitress | 代码量小，适合 Excel 与轻量业务接口 |
| 数据库 | PostgreSQL | 支持服务器长期运行、事务与并发访问 |
| Excel | openpyxl | 解析聚水潭 `.xlsx` 并生成发货清单 |
| 部署 | Docker Compose | Mac、服务器运行方式一致，便于重启和更新 |
| 公网入口 | Caddy | 自动 HTTPS、反向代理和全站密码验证 |
| 版本管理 | Git | 试运行分支验收后再合并正式版本 |

## 5. 数据设计

- `batches`：批次号、内部单号、来源文件、创建与更新时间。
- `skus`：批次内商品编码、商品名、款式、颜色尺码、仓位、清单数量、搜索聚合字段。
- `packages`：大包号、可选长宽高重、所属批次。
- `packing_ratios`、`packing_ratio_items`：批次内配比及其 SKU 组成；删除采用停用方式保留历史关联。
- `package_entries`、`package_entry_items`：保存时固化普通 SKU/配比名称、每中包件数、中包数量及组成快照。
- `package_items`：展开后的大包与 SKU 实际数量，是库存统计的统一口径。

关键约束：

- 批次号全局唯一。
- 同一批次的大包号唯一。
- 同一批次的商品编码唯一。
- 同一大包内同一 SKU 只保存一条，重复添加时合并数量。
- 批量生成大包使用数据库事务；任一大包号冲突时整次操作不落库。
- 配比只能引用所属批次的 SKU，不能跨批次使用。
- 修改或删除配比不追改已经保存的大包；查看、复制和导出均读取保存时快照。

## 6. 库存口径

系统只校验本次导入批次，不连接或校验聚水潭总库存。

```text
库存 = 本批次清单数量 - 本批次已保存数量
录入参考库存 = 库存 - 当前编辑区尚未保存的数量
配比实际用量 = 每中包内某 SKU 件数 × 中包数量 × 同款大包数
体积(m³) = 长(cm) × 宽(cm) × 高(cm) ÷ 1,000,000
```

库存可以为负数，代表本批次已超装。超装是软提醒，不是强制拦截。

## 7. Excel 导出

导出文件名：`发货清单_批次号.xlsx`，服务器响应中附带精确到分钟的实际生成文件名。

导出只包含 `发货清单` 一个工作表。每个大包只占一行，普通款色尺码和配比明细在同一单元格内分行；使用配比时增加“中包件数、中包数量、总数量”。散件数量只出现在“数量”列，配比只出现在中包列。重量后增加根据长宽高派生的体积列。SKU 库存核对保留在网页，不重复导出。

自动循环配比第一版采用均衡模式：用户选择当前批次剩余 SKU、起始大包号和大包数量，系统先平均分配每个 SKU，再把余数轮换放入当前总件数较少的大包。预览不写库，确认时重新锁定批次并校验预览令牌；库存或参数发生变化时必须重新预览。所有新大包在一个事务中生成，任一号码冲突则整体不落库。

导出接口返回标准 `.xlsx` 文件，并设置禁止缓存，避免下载到旧批次内容。

## 8. 安全方案

- Caddy 在请求进入 Flask 前执行 HTTP Basic Auth；HTML、静态文件、上传、下载和全部 API 使用同一组网页凭据。
- 网页允许任意公网来源访问，但未提供正确密码的请求只返回 `401`，不会进入业务应用。
- 网页密码只以 bcrypt 哈希形式保存在服务器 `.env`，代码仓库不保存用户名以外的真实凭据。
- 浏览器可保存这组凭据，日常使用不需要反复输入；更换密码后，原凭据立即失效。
- 所有写操作校验 CSRF Token。
- SQL 用户值全部通过参数绑定传入，不直接拼接到 SQL。
- PostgreSQL 不开放公网端口。
- 应用端口只监听服务器回环地址。
- 启用 CSP、禁止页面被嵌入、禁止 MIME 猜测、关闭摄像头/麦克风/定位权限。
- `.env`、数据库、上传文件、备份、Excel 和本地运行环境不进入 Git。
- 上传文件限制为 `.xlsx`，请求体最大 20 MB。

注意：统一密码主要防止普通扫描器和匿名脚本调用业务接口，不等同于抵御大流量 DDoS。网页凭据应只在业务人员之间传递，发生泄露时立即更换密码哈希并重启 Caddy。

## 9. 部署与数据持久化

服务器目录为 `/opt/shipping-list`，运行：

```bash
docker compose up -d --build
docker compose ps
```

数据保存在 Docker 命名卷：

- `postgres-data`：PostgreSQL 数据。
- `packing-uploads`：上传与应用数据目录。
- `caddy-data`、`caddy-config`：HTTPS 证书和 Caddy 状态。

`docker compose down` 不会删除数据；禁止执行 `docker compose down -v`。

备份脚本 `backup-postgres.sh` 使用 `pg_dump` 生成压缩文件，默认保留 7 天。网页可下载最近7份备份到本地；恢复前必须停止业务写入并先备份当前数据库。

## 10. 测试与上线门槛

每次上线前必须通过：

```bash
.venv/bin/python -m unittest discover -s tests -v
npm run test:frontend
docker compose config --quiet
docker compose ps
```

当前自动测试覆盖：

- Excel 导入、内部单号、批次生成和历史查询。
- 多 SKU 大包、同款大包复制、冲突时事务回滚。
- 超装提醒、强制保存、修改、删除和库存返还。
- 长宽高重留空、CSRF 与安全响应头。
- Excel 单工作表、一大包一行、散件与中包分列及表头数据。
- 款色尺码点击展开、多关键词搜索、添加后收起、库存扣减与返还。
- 导出地址和下载文件名。
- 配比增删改、批次隔离、普通 SKU 与多个配比混装。
- 配比库存展开、同款大包倍增、超量提醒和强制保存。
- 配比历史快照，以及普通批次/配比批次两种 Excel 结构。
- 体积计算、体积缺失提示、Excel体积列和一线稳定SKU排序。
- 自动循环配比数量守恒、均衡分配、部分SKU、号码冲突、库存变化及事务回滚。
- PostgreSQL环境20个并发写入、相同大包号冲突、7天备份及网页下载校验。

试运行验收通过后：

1. 功能分支完成回归后合并到 `main` 并打版本标签。
2. 服务器部署对应版本标签并重新构建应用容器。
3. 再次执行健康检查、真实导入、录入、导出冒烟测试。
4. 验证不同网络均能看到密码提示，匿名请求无法访问页面和 API。

## 11. 当前限制

- 只有一组全站共享凭据，不含个人账号、角色、密码找回和操作审计。
- 不连接聚水潭 API，只处理人工导入的拣货单。
- 不校验企业总库存，只校验当前批次。
- 多人同时编辑同一大包时不做前端锁定，数据库唯一约束负责阻止重复大包号。
- 公网 HTTPS 首次启用前，阿里云安全组必须允许 80、443，以便证书验证和用户访问。
